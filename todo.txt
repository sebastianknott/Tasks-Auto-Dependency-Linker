(D) Prepare submission PR to obsidian-releases community-plugins.json +release @deploy
(B) Cache blind spot: 'ID' typed into another open but unsaved file is invisible because vault modify fires on save only +bug @dev
(B) Stale suppression: LineWriteArbiter keeps suppressedTypes when a file's whole content is replaced externally (sync, git checkout, vault overwrite) while the cursor stays on the same line index, silently blocking marker writes on unrelated new content. beginPass only resets on filePath or cursorLine change +bug @dev
(C) Restructure code to bettest reflect low coupling, high cohesion +refactor @dev
(C) Add trigger(name, ...args) to EventEmitterStub in tests/__mocks__/obsidian.ts, replacing getHandlers(name)[0].cb(arg) call sites. Own mutation-gated commit: firing all handlers instead of [0] is a behaviour change +refactor @dev
